# Runtime mode: mock (default) or real
MA_OFFICE_MODE=mock

# Auto-detected from `codex --help` in this repo: `codex mcp-server`
# Override only if your local Codex CLI differs.
MA_OFFICE_MCP_COMMAND=codex mcp-server

# Real mode transport (currently only stdio)
MA_OFFICE_MCP_TRANSPORT=stdio

# Recommended in real mode: Linux filesystem path in WSL (not /mnt/*)
# Example: /home/you/maOffice
# MA_OFFICE_WORKDIR=/home/you/maOffice

# Optional override for base branch used in GITHUB stage
# MA_OFFICE_BASE_BRANCH=main

# Required if your Codex auth flow uses API key
# OPENAI_API_KEY=sk-...
